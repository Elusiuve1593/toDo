import { createSlice } from "@reduxjs/toolkit";
//import { createRegistration, RegistrationInfo } from "./operations";

const initialState = {};

const todoSlice = createSlice({
  name: "todo",
  initialState,
  reducers: {},
});

export default todoSlice.reducer;
